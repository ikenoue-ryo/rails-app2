<h1>詳細画面</h1>
<a href="/boards">
  <%= link_to 'Edit', edit_board_path(@board), class: 'ui right floated button' %>
  <%= link_to 'List', boards_path, class: 'ui right floated button' %>
</a>
<br><br>

<% if flash[:notice] %>
  <div class="ui blue message"><%= flash[:notice] %></div>
  <% end %>
<%= render partial: 'board', object: @board %>

<%= render @board.comments %>

<%= render partial: 'comments/form', locals: { comment: @comment } %>
